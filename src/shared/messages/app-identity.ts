/**
 * The product name is a brand, not copy: it renders verbatim under every
 * locale (matching the static <title> in index.html) and stays out of the
 * message catalogs.
 */
export const APP_NAME = 'Room Layout'
